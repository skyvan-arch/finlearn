# Neon PostgreSQL — use same URL for both local dev and production
# Get this from Vercel → Storage → your Neon database → .env.local tab
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
